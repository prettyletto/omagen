Plan:
1. Refactor Go backend into cli, session, and omarchy packages with a wiring-only main.go
2. Extract QML into state, services, and views and rewrite Omagen.qml as the orchestrator
3. Update install.sh and .gitignore for the new backend and qml directory layout
4. Build, vet, and validate the Go backend and QML files with no behavior change